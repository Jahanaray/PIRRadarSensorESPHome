# ESPHome Secrets Example File
# Copy this file to secrets.yaml and replace placeholder values with your own.
# NEVER commit secrets.yaml to git.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

api_encryption_key: "YOUR_API_ENCRYPTION_KEY"

ota_username: "ota_user"
ota_password: "CHANGE_ME_OTA_PASSWORD"

fallback_wifi_ssid: "SecurityNode Fallback"
fallback_wifi_password: "CHANGE_ME_FALLBACK_WIFI_PASSWORD"
